# `pnpm env:local` generates a local .env with strong secrets. Never commit .env.
COMPOSE_PROJECT_NAME=play-together
PLATFORM_NETWORK_NAME=play-together_platform
PLATFORM_NETWORK_EXTERNAL=false
BIND_ADDRESS=127.0.0.1

# Local-only host ports from docker-compose.local.yml.
WEB_PORT=4173
REALTIME_PORT=8787
GAME_CDN_PORT=8081
CONVEX_PORT=43210
CONVEX_SITE_PORT=43211
CONVEX_DASHBOARD_PORT=46791

# Browser-visible endpoints. Production values must use HTTPS/WSS.
VITE_CONVEX_URL=http://convex.localhost:43210
VITE_REALTIME_URL=ws://localhost:8787/v1/connect
GAME_CDN_PUBLIC_ORIGIN=http://localhost:8081

# Exact public origins accepted by the realtime gateway and game registry.
ALLOWED_ORIGINS=http://localhost:4173
GAME_MODULE_ORIGINS=http://localhost:8081

# Optional public-to-private fetch routing for containers. Use {} when public DNS
# is reachable from the services.
GAME_MODULE_FETCH_ORIGIN_MAP='{"http://localhost:8081":"http://game-cdn:8080"}'
ALLOW_INSECURE_GAME_ORIGINS=true

# Generate, never commit, and keep identical in Convex + realtime.
JOIN_TICKET_SECRET=
GAME_PUBLISH_TOKEN=

# Self-hosted Convex.
CONVEX_SELF_HOSTED_URL=http://127.0.0.1:43210
CONVEX_INSTANCE_NAME=play-together-local
CONVEX_INSTANCE_SECRET=
CONVEX_CLOUD_ORIGIN=http://convex.localhost:43210
CONVEX_SITE_ORIGIN=http://convex-site.localhost:43211
REDACT_LOGS_TO_CLIENT=true
DISABLE_METRICS_ENDPOINT=false
DISABLE_BEACON=true
DOCUMENT_RETENTION_DELAY=172800

# Realtime safeguards.
ROOM_IDLE_TIMEOUT_MS=30000
MAX_PAYLOAD_BYTES=65536
